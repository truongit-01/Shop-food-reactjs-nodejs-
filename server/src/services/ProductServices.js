// const CardProduct = require('../models/CardProductsModel');

// const getProductById = (id) => {
//     CardProduct.find({ '_id': { $in: productIds } })
//         .populate('writer')
//         .exec((err, product) => {
//             if (err) return req.status(400).send(err)
//             return res.status(200).send(product)
//         })

// };

// module.exports = {
//     getProductById,
// }

const handleProductLimit = () => {
    
}
